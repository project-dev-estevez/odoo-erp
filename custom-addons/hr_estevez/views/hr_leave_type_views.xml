<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Añadir campo is_vacation al formulario de tipos de tiempo personal -->
    <record id="view_hr_leave_type_form_inherit" model="ir.ui.view">
        <field name="name">hr.leave.type.form.inherit</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_status_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='requires_allocation']" position="after">
                <field name="is_vacation"/>
            </xpath>
        </field>
    </record>

    
</odoo>