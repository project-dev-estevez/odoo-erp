<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    
    <t t-name="hr_recruitment_estevez.RecruiterEfficiencyChart" owl="1">
        <div class="recruiter-efficiency-chart">
            <!-- ✅ Gráfico o Loading -->
            <div t-if="state.isLoading" class="text-center py-5">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Cargando...</span>
                </div>
                <div class="mt-2 text-muted">Cargando datos de eficiencia...</div>
            </div>

            <div t-else="">
                <ChartRendererApex 
                    type="'bar'"
                    title="props.title || 'Eficiencia por Reclutador'"
                    config="state.chartData"
                    height="props.height || 400"/>
                
                <!-- 💡 TIP -->
                <div class="mt-3">
                    <div class="alert alert-info border-0 shadow-sm">
                        <div class="d-flex align-items-start">
                            <i class="fa fa-lightbulb me-3 mt-1 text-primary"></i>
                            <div>
                                <strong>💡 Tips:</strong>
                                <div class="mt-1 small">
                                    <p class="mb-2">Los resultados en la gráfica solo tienen en cuenta los postulantes que superaron la etapa de "Primera Entrevista".</p>
                                    <p class="mb-0">Esta gráfica muestra qué tan efectiva es la perfilación inicial del área de reclutamiento para llevar candidatos desde la primera entrevista hasta la contratación.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>

</templates>