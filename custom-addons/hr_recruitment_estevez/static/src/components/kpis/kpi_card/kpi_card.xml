<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    
    <t t-name="hr_recruitment_estevez.KpiCard" owl="1">
        <div class="h-100">
            <!-- ✅ 100% Bootstrap utilities -->
            <div class="card h-100 border-1 shadow-sm bg-white text-center position-relative rounded-3"
                t-att-class="props.onClick ? 'cursor-pointer' : ''"
                t-on-click="onCardClick"
                onmouseover="this.classList.replace('shadow-sm', 'shadow-lg'); this.style.transform='translateY(-0.5rem) scale(1.02)'"
                onmouseout="this.classList.replace('shadow-lg', 'shadow-sm'); this.style.transform='translateY(0) scale(1)'"
                style="transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">
                
                <div class="card-body d-flex flex-column justify-content-center p-4 position-relative">
                    
                    <!-- ✅ Badge premium en esquina superior derecha -->
                    <div t-if="props.showSecondaryValue and props.secondaryValue > 0" 
                         class="position-absolute top-0 end-0 m-2">
                        <span class="badge rounded-pill px-2 py-1 fs-7 fw-normal"
                              style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);">
                            <i class="bi bi-clock-history me-1"></i>
                            <span style="font-weight: 600;"><t t-esc="props.secondaryValue"/></span>
                        </span>
                    </div>

                    <!-- ✅ Valor principal grande -->
                    <div class="display-3 fw-bold text-primary mb-2 lh-1">
                        <t t-esc="formattedValue"/>
                    </div>
                    
                    <!-- ✅ Título principal -->
                    <h6 class="text-muted fw-medium text-uppercase ls-1 mb-0 opacity-75">
                        <t t-esc="props.name"/>
                    </h6>
                    
                    <!-- ✅ Indicador sutil de valor secundario -->
                    <div t-if="props.showSecondaryValue and props.secondaryValue > 0" 
                        class="mt-2"
                        style="font-size: 1.1rem;">
                        <i class="bi bi-plus-circle me-1" style="color: #667eea;"></i>
                        <span style="font-weight: 700; color: #667eea;">
                            <t t-esc="props.secondaryValue"/> adicionales
                        </span>
                    </div>

                </div>
            </div>
        </div>
    </t>

</templates>